<UserControl x:Class="PacMan.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PacMan.Views"
             xmlns:vm ="clr-namespace:PacMan.ViewModels"
             xmlns:components="clr-namespace:PacMan.Views.Components"
             d:DataContext="{d:DesignInstance vm:GameViewModel}"
             mc:Ignorable="d" Height="550" Width="784">
    <Border
        Background="White"
        BorderBrush="Black"
        BorderThickness="3"
        CornerRadius="5">
        <Grid>
            <ItemsControl Width="auto" Height="auto" ItemsSource ="{Binding GameMap}" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="1" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="20" Rows="20" MinHeight="500" MinWidth="500"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Border>

</UserControl>
